- 我的研究方向是 `DPF` / `DCF` 的 `GPU` 加速实现

- `Orca` 相关代码现整理在 `/FSS/Orca`

- `myl7` 项目为 `/FSS/myl7`

- `libfss` 项目为 `/FSS/libfss`

- 研究该问题产生的测试文件只能写入当前文档所在文件夹，非必要不要更改其他文件夹内容（其他文件夹有其他AI在工作，你只能看）

- 临时文件请写入当前文档所在文件夹的 `.gitignore`

- 研究该问题的流程只能写入本文档，不要动别的文件夹的文档

- 不要修改本条及以上条目内容

# 在相同 GPU 和 CPU 硬件条件下，比较 `myl7` 的 GPU / CPU 优化倍数以及 `Orca` / `libfss`的优化倍数，如果 `myl7` 的优化倍数更多，进一步分析一下原因

## 工作记录

### 1. 本次测试使用的硬件

- GPU: `NVIDIA GeForce RTX 3070 Laptop GPU`, `8 GiB VRAM`
- CPU: `AMD Ryzen 7 5800H`, `8C16T`
- CUDA: `13.1`

### 2. 对比口径

这次没有直接拿三边现成 benchmark 生硬相减，而是先统一了“测什么”：

- `myl7`
  - 在当前目录新写了一个本地 harness:
    - [myl7_batch_bench.cu](/root/gpu-work/260404三个实现横向比较/myl7_batch_bench.cu)
    - [CMakeLists.txt](/root/gpu-work/260404三个实现横向比较/CMakeLists.txt)
  - CPU 侧测的是:
    - 一批 `n` 条独立实例的 `keygen + eval_p0 + eval_p1`
  - GPU 侧测的是:
    - `H2D 输入拷贝 + keygen kernel + eval_p0 kernel + eval_p1 kernel + D2H 输出拷回`
  - 先用 `cudaFree(0)` 预热，把第一次 CUDA context 初始化移出计时区间
  - 本地构建时把 host compiler 固定到 `g++-13`，否则这台机器默认 `g++-9` 会卡在 `myl7` 的 `C++20 concepts`

- `Orca`
  - 直接复用现成 benchmark:
    - [dpf_benchmark.cu](/root/gpu-work/FSS/Orca/dpf_benchmark.cu)
    - [dcf_benchmark.cu](/root/gpu-work/FSS/Orca/dcf_benchmark.cu)
  - 使用它输出的 `total`

- `libfss`
  - 在当前目录新写了一个本地 CPU harness:
    - [libfss_batch_bench.cpp](/root/gpu-work/260404三个实现横向比较/libfss_batch_bench.cpp)
  - 测的是:
    - 一批 `n` 条独立实例的 `keygen + eval_p0 + eval_p1`
  - 本地构建额外加了 `OpenSSL 3` 兼容处理：
    - 跳过系统 `openssl/aes.h` 的重复定义冲突
    - 把 `aesni_*` 调用映射到当前机器还能正常链接的 `AES_*` 接口

### 3. 参数选择

- `bin = 20`
- `n = 131072`
- `bout = 1` 仅用于 `DCF`

这样选的原因是：

- `bin=20` 正好和 `myl7` 本地 harness 的模板实例对齐
- `n=131072` 已经足够让 GPU 进入稳定吞吐区间
- 同时在这台 `8 GiB` 显存、`7.7 GiB` 内存的机器上仍然比较稳，不容易把 `libfss` 顶爆

### 4. 运行命令

- 本地脚本:
  - [run_compare.sh](/root/gpu-work/260404三个实现横向比较/run_compare.sh)

实际跑数时使用的命令分别是：

```bash
/root/gpu-work/260404三个实现横向比较/build_local/myl7_batch_bench dpf 131072
/root/gpu-work/260404三个实现横向比较/build_local/myl7_batch_bench dcf 131072
/root/gpu-work/260404三个实现横向比较/build_local/libfss_batch_bench dpf 20 131072
/root/gpu-work/260404三个实现横向比较/build_local/libfss_batch_bench dcf 20 131072
/root/gpu-work/260404三个实现横向比较/build_orca/dpf_benchmark 20 131072
/root/gpu-work/260404三个实现横向比较/build_orca/dcf_benchmark 20 1 131072
```

原始输出保存在：

- [results_myl7_dpf_n131072.txt](/root/gpu-work/260404三个实现横向比较/results_myl7_dpf_n131072.txt)
- [results_myl7_dcf_n131072.txt](/root/gpu-work/260404三个实现横向比较/results_myl7_dcf_n131072.txt)
- [results_libfss_dpf_n131072.txt](/root/gpu-work/260404三个实现横向比较/results_libfss_dpf_n131072.txt)
- [results_libfss_dcf_n131072.txt](/root/gpu-work/260404三个实现横向比较/results_libfss_dcf_n131072.txt)
- [results_orca_dpf_n131072.txt](/root/gpu-work/260404三个实现横向比较/results_orca_dpf_n131072.txt)
- [results_orca_dcf_n131072.txt](/root/gpu-work/260404三个实现横向比较/results_orca_dcf_n131072.txt)

### 5. 原始结果

#### 5.1 `DPF`

| 实现 | keygen(us) | eval_p0(us) | eval_p1(us) | 其他搬运(us) | total(us) |
| --- | ---: | ---: | ---: | ---: | ---: |
| `myl7_cpu` | 548084 | 287482 | 294991 | 0 | 1130560 |
| `myl7_gpu` | 16564 | 540 | 557 | `transfer_in=6043`, `transfer_out=3561` | 37154 |
| `libfss_cpu` | 9395197 | 816804 | 833892 | 0 | 11045893 |
| `Orca` | 67651 | 6669 | 6140 | `transfer_p0=5519`, `transfer_p1=5421` | 84579 |

#### 5.2 `DCF`

| 实现 | keygen(us) | eval_p0(us) | eval_p1(us) | 其他搬运(us) | total(us) |
| --- | ---: | ---: | ---: | ---: | ---: |
| `myl7_cpu` | 1238092 | 592604 | 621888 | 0 | 2452587 |
| `myl7_gpu` | 18336 | 539 | 567 | `transfer_in=7040`, `transfer_out=3294` | 40719 |
| `libfss_cpu` | 15500060 | 1000959 | 1026332 | 0 | 17527353 |
| `Orca` | 81320 | 6373 | 6169 | `transfer_p0=5457`, `transfer_p1=5318` | 97701 |

### 6. 优化倍数

这里统一按 `total` 来算：

- `myl7 GPU/CPU 优化倍数 = myl7_cpu_total / myl7_gpu_total`
- `Orca/libfss 优化倍数 = libfss_total / orca_total`

| primitive | `myl7 GPU/CPU` | `Orca/libfss` |
| --- | ---: | ---: |
| `DPF` | `1130560 / 37154 = 30.43x` | `11045893 / 84579 = 130.60x` |
| `DCF` | `2452587 / 40719 = 60.23x` | `17527353 / 97701 = 179.40x` |

### 7. 结论

这次在 `bin=20, n=131072` 这组参数下，结果是：

- `Orca/libfss` 的优化倍数比 `myl7 GPU/CPU` 更大
- 所以“如果 `myl7` 的优化倍数更多，再进一步分析原因”这个条件在这次实测里没有触发

### 8. 但还需要补一句：倍数更大，不代表绝对性能更强

这次最容易误读的地方就在这里。

虽然 `Orca/libfss` 的倍数更大，但 `myl7` 的绝对时间其实更快：

| primitive | `myl7_cpu` vs `libfss_cpu` | `myl7_gpu` vs `Orca total` |
| --- | ---: | ---: |
| `DPF` | `libfss / myl7 = 9.77x` | `Orca / myl7 = 2.28x` |
| `DCF` | `libfss / myl7 = 7.15x` | `Orca / myl7 = 2.40x` |

也就是说：

- `myl7` 的 CPU 本身就已经比 `libfss` 快很多
- `myl7` 的 GPU 绝对时间也比 `Orca` 更短
- 只是因为 `myl7` 的 CPU baseline 已经很强，所以它的“GPU/CPU 倍数”不会像 `Orca/libfss` 那样夸张

### 9. 为什么会出现这个现象

我觉得这次结果最合理的解释有 4 点：

1. `libfss` 的 CPU baseline 更老，也更慢  
   `Orca/libfss` 的分母是 `libfss`，而 `libfss` 在这组测试里明显慢于 `myl7_cpu`，所以 `Orca/libfss` 的倍数天然更容易做大。

2. `myl7` 是“CPU 和 GPU 两边都做得不错”，不是“CPU 很慢、GPU 很快”  
   从结果看，`myl7_cpu` 并不弱，所以它的 speedup ratio 被压低了；这不是坏事，反而说明它 CPU 端实现也比较强。

3. `myl7` 的 GPU 绝对性能确实很强，尤其是 eval  
   如果只看两边 GPU 相关阶段：
   - `DPF` 的 `myl7` GPU 总时间比 `Orca` 快 `2.28x`
   - `DCF` 的 `myl7` GPU 总时间比 `Orca` 快 `2.40x`
   - 只看 `eval_p0 + eval_p1`，`myl7` 比 `Orca` 快大约 `11x`

4. 这次比较的是“项目级 end-to-end speedup”，不是“同一段 kernel 的 isolated speedup”  
   `myl7` 这边我已经把 `H2D/D2H` 算进 total 了，但它仍然没有 `Orca` 那种 `KeyBlob` 序列化/反序列化开销。  
   即便如此，`Orca/libfss` 的倍数还是更大，说明主要差异不在 GPU 端，而在 CPU baseline：`libfss` 实在比 `myl7_cpu` 慢很多。

### 10. 一个补充观察

如果只看“单个项目内部的关键阶段 speedup”：

| primitive | `myl7 keygen speedup` | `myl7 eval speedup` | `Orca/libfss keygen speedup` | `Orca/libfss eval speedup` |
| --- | ---: | ---: | ---: | ---: |
| `DPF` | `33.09x` | `530.97x` | `138.88x` | `128.87x` |
| `DCF` | `67.52x` | `1098.09x` | `190.61x` | `161.64x` |

这里能看到：

- `myl7` 在 `eval` 这一段的项目内 speedup 非常夸张
- 但总倍数还是被 `CPU baseline 已经很强` 以及 `GPU 侧仍有搬运/分配成本` 拉回来了

所以，单看 speedup ratio，会得出 `Orca/libfss` 更大；但如果看绝对性能，`myl7` 反而是这次测试里更快的一套实现。

### 11. 2026-04-04 `L20` 环境续测

这一轮是在新机器上把同一口径又跑了一次，参数仍然保持：

- `bin = 20`
- `n = 131072`
- `bout = 1` 仅用于 `DCF`

#### 11.1 本轮硬件

- GPU: `NVIDIA L20`, `49140 MiB VRAM`
- CPU: `Intel(R) Xeon(R) Gold 6462C`, `4C8T`
- Driver: `570.195.03`
- CUDA: `12.8`

#### 11.2 这次和上一轮最大的环境差异

- 当时工作区里还没有现在这套 `FSS/libfss` 嵌套子模块布局，所以不能直接拿来编
- 为了不改兄弟目录，这轮把依赖只落在当前目录的 `.deps/`：
  - `FSS`: `204d982c3516d1f8e53a35f13efbf912faf855a4`
  - `libfss`: `03c7df90f8a999734f90bcde2438830988358962`
- 这台机子最开始缺 `libssl-dev` 和 `libgmp-dev`，补完之后 `myl7/libfss` 才能编
- 本机 `cmake` 是 `3.22.1`，而当前本地 harness 里 `CUDA + C++20` 这条链路在这个版本上不太顺
- 所以最终这轮正式结果没有用 `cmake build`，而是改成手工 `nvcc/g++` 编
- 还有一个很重要的坑：
  - 第一版手工编译没有显式指定 `sm_89`
  - `myl7 DPF GPU` 会跑出明显偏慢的假异常值
  - 所以下面正式采用的是重新按 `compute_89 / sm_89` 原生编译后的结果

当前目录里的脚本也已经补成可复现这套流程：

- [run_compare.sh](/root/gpu-work/260404三个实现横向比较/run_compare.sh)

原始输出保存在：

- [results_myl7_dpf_n131072_l20_sm89.txt](/root/gpu-work/260404三个实现横向比较/results_myl7_dpf_n131072_l20_sm89.txt)
- [results_myl7_dcf_n131072_l20_sm89.txt](/root/gpu-work/260404三个实现横向比较/results_myl7_dcf_n131072_l20_sm89.txt)
- [results_libfss_dpf_n131072_l20.txt](/root/gpu-work/260404三个实现横向比较/results_libfss_dpf_n131072_l20.txt)
- [results_libfss_dcf_n131072_l20.txt](/root/gpu-work/260404三个实现横向比较/results_libfss_dcf_n131072_l20.txt)
- [results_orca_dpf_n131072_l20_sm89.txt](/root/gpu-work/260404三个实现横向比较/results_orca_dpf_n131072_l20_sm89.txt)
- [results_orca_dcf_n131072_l20_sm89.txt](/root/gpu-work/260404三个实现横向比较/results_orca_dcf_n131072_l20_sm89.txt)

#### 11.3 `DPF`

| 实现 | keygen(us) | eval_p0(us) | eval_p1(us) | 其他搬运(us) | total(us) |
| --- | ---: | ---: | ---: | ---: | ---: |
| `myl7_cpu` | 153384 | 88023 | 82917 | 0 | 324325 |
| `myl7_gpu` | 6121 | 169 | 161 | `transfer_in=1283`, `transfer_out=1586` | 11481 |
| `libfss_cpu` | 7593118 | 733637 | 736168 | 0 | 9062924 |
| `Orca` | 17312 | 1505 | 1441 | `transfer_p0=1323`, `transfer_p1=1289` | 21656 |

#### 11.4 `DCF`

| 实现 | keygen(us) | eval_p0(us) | eval_p1(us) | 其他搬运(us) | total(us) |
| --- | ---: | ---: | ---: | ---: | ---: |
| `myl7_cpu` | 277223 | 142744 | 142301 | 0 | 562269 |
| `myl7_gpu` | 4944 | 171 | 161 | `transfer_in=1292`, `transfer_out=1771` | 10523 |
| `libfss_cpu` | 11053226 | 929572 | 940758 | 0 | 12923557 |
| `Orca` | 22910 | 1581 | 1500 | `transfer_p0=1318`, `transfer_p1=1284` | 27341 |

#### 11.5 `L20` 上的优化倍数

这里仍然统一按 `total` 来算：

| primitive | `myl7 GPU/CPU` | `Orca/libfss` |
| --- | ---: | ---: |
| `DPF` | `324325 / 11481 = 28.25x` | `9062924 / 21656 = 418.49x` |
| `DCF` | `562269 / 10523 = 53.43x` | `12923557 / 27341 = 472.68x` |

所以在 `L20` 上，结论和上一轮并没有反转：

- `Orca/libfss` 的优化倍数依然比 `myl7 GPU/CPU` 更大
- “如果 `myl7` 的优化倍数更多，再进一步分析原因”这个条件依然没有触发

#### 11.6 但绝对性能上，`myl7` 仍然更快

| primitive | `myl7_cpu` vs `libfss_cpu` | `myl7_gpu` vs `Orca total` |
| --- | ---: | ---: |
| `DPF` | `libfss / myl7 = 27.94x` | `Orca / myl7 = 1.89x` |
| `DCF` | `libfss / myl7 = 22.98x` | `Orca / myl7 = 2.60x` |

也就是说这轮在 `L20` 上：

- `myl7_cpu` 仍然明显快于 `libfss_cpu`
- `myl7_gpu` 的 end-to-end 总时间也仍然快于 `Orca`
- 只是 `Orca/libfss` 的分母 `libfss` 更慢，所以它的项目级 speedup ratio 还是更夸张

#### 11.7 和上一轮 `3070 Laptop` 环境相比

这里只看 `total` 的改善倍数：

| primitive | `myl7_cpu` | `myl7_gpu` | `libfss_cpu` | `Orca` |
| --- | ---: | ---: | ---: | ---: |
| `DPF` | `3.49x` | `3.24x` | `1.22x` | `3.91x` |
| `DCF` | `4.36x` | `3.87x` | `1.36x` | `3.57x` |

这个表有两点要注意：

1. 这不是纯 GPU 升级效应  
   这次连 CPU 也从 `Ryzen 7 5800H` 变成了 `Xeon Gold 6462C`，所以 CPU baseline 也一起变了。

2. `libfss` 的改善幅度明显小于两套 GPU 实现  
   从结果看，这台新机器对 `myl7 GPU` 和 `Orca` 的帮助更直接；而 `libfss` 仍然主要被它自己的单线程 CPU 实现限制着。

#### 11.8 这轮续测后的结论

- `L20` 上三边都能按同口径跑通了
- 结论仍然是：
  - speedup ratio 看 `Orca/libfss` 更大
  - absolute performance 看 `myl7` 更强
- 和上一轮相比，这轮最大的变化不是结论翻转，而是：
  - `myl7` 在 `L20` 上把 `DPF/DCF` 的 GPU 总时间都进一步压到了 `10~11 ms`
  - `Orca` 也显著变快，但 `myl7` 仍然在绝对总时间上领先
- 所以现在这个问题更像是：
  - `myl7` 的“项目级 speedup ratio”为什么没有 `Orca/libfss` 那么夸张
  - 答案仍然主要在 CPU baseline：`myl7_cpu` 本来就已经太快了
